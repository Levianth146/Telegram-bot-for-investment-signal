# Telegram bot (từ @BotFather). Không commit file .env thật.
BOT_TOKEN=changeme
# SQLite store — bot và pipeline cùng path này
DATABASE_PATH=store/bot.db
# Tuỳ chọn: vendor API key nếu provider yêu cầu
DATA_SOURCE_API_KEY=changeme
