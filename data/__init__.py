"""Data ingest package — only place pipeline should pull network/vendor data.

``fundamental_filter/`` and ``quant_engine/`` receive prepared frames/series.
See ``data/providers/`` and ``pipeline/config.yaml`` ``data_sources``.
"""
